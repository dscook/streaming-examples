ext {
    scalaVersion = '2.11'
    scalaVersionFull = "$scalaVersion.8"
}

allprojects {
    group = 'uk.co.bitcat.streaming'
    version = '0.1'
}

subprojects {
    apply plugin: 'scala'

    repositories {
        mavenCentral()
    }
}